name: Release Stable

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - '!v[0-9]+.[0-9]+.[0-9]+-*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Stable tag to release (e.g., v0.2.0)'
        required: true

permissions:
  contents: write
  id-token: write  # Required for attestations
  attestations: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-python-wheels:
    name: Build Python Wheels
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        target: [x86_64, aarch64]
        exclude:
          - os: windows-latest
            target: aarch64
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.11'

      - name: Build wheels
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b
        with:
          working-directory: rust/marty-rs
          target: ${{ matrix.target }}
          args: --locked --release --out ../../dist --features python
          sccache: 'true'
          manylinux: auto

      - name: Upload wheels
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.target }}
          path: dist/*.whl

  build-wasm:
    name: Build WASM Packages
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rust/marty-rs
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM packages
        run: |
          mkdir -p ../../dist/wasm
          
          wasm-pack build --locked --target web --no-default-features --features wasm --out-dir ../../dist/wasm/web
          wasm-pack build --locked --target nodejs --no-default-features --features wasm --out-dir ../../dist/wasm/nodejs
          wasm-pack build --locked --target bundler --no-default-features --features wasm --out-dir ../../dist/wasm/bundler

      - name: Create WASM archive
        run: |
          cd ../../dist
          tar -czf marty-rs-wasm.tar.gz wasm/
          sha256sum marty-rs-wasm.tar.gz > checksums-wasm.txt

      - name: Generate SBOM
        run: |
          cargo install cargo-cyclonedx
          cargo cyclonedx --format json --output-cdx ../../dist/marty-rs-sbom.json

      - name: Upload WASM artifacts
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: wasm-artifacts
          path: |
            dist/marty-rs-wasm.tar.gz
            dist/checksums-wasm.txt
            dist/marty-rs-sbom.json

  build-source-dist:
    name: Build Python Source Distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.11'

      - name: Install build tools
        run: pip install build hatch

      - name: Build source distribution
        run: python -m build --sdist --outdir dist

      - name: Upload source dist
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: source-dist
          path: dist/*.tar.gz

  test:
    name: Run Tests Before Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4

      - name: Run Rust tests
        working-directory: rust/marty-rs
        run: cargo test --locked --all-features

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.11'

      - name: Install and test Python package
        run: |
          pip install maturin[patchelf] pytest
          cd rust/marty-rs
          maturin build --locked --features python --interpreter python --out dist
          python -c "import pathlib, subprocess, sys; subprocess.check_call([sys.executable, '-m', 'pip', 'install', str(next(pathlib.Path('dist').glob('*.whl')))])"
          cd ../..
          pip install -e .
          pytest tests/

  create-release:
    name: Create Stable Release
    needs: [build-python-wheels, build-wasm, build-source-dist, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - name: Get version from tag
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=${TAG#v}" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131
        with:
          path: release-artifacts

      - name: Consolidate artifacts
        run: |
          mkdir -p final-release
          find release-artifacts -type f -exec cp {} final-release/ \;
          cd final-release
          sha256sum * > SHA256SUMS

      - name: Generate release notes
        run: |
          cargo install git-cliff
          git cliff --tag ${{ steps.version.outputs.tag }} --unreleased --strip all > RELEASE_NOTES.md

      - name: Generate attestations
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a
        with:
          subject-path: 'final-release/*.tar.gz'

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.version }}
          body_path: RELEASE_NOTES.md
          files: final-release/*
          prerelease: false
          generate_release_notes: true
